package com.epicness.game;

import com.badlogic.gdx.graphics.g2d.SpriteBatch;

public class GunRunWorld {


    public void draw(SpriteBatch batch) {
        // We call draw(batch) on every object participating in GunRunWorld
    }

    public void update(float delta) {
        // Here we update the state of the game before rendering it
    }
}
